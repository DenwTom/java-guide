package com.example.benchmark.models;

import java.util.Collection;

public interface IOperations {
    void addElements(Collection collection);
    void findElements(Collection collection);
    void removeElements(Collection collection);
}
